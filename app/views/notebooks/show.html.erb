

<div class="wrapper_with_padding">
  <div id="note_show">
    
    <h1><%=@notebook.title%></h1>
      

      <!-- <div class= "buttons">
      link_to "Edit", edit_note_path(@note), class: "button"
       link_to "Delete", note_path(@note), method: :delete, data: { confirm: "Are you sure?" }, class: "button"  -->


<!-- 
<div class="wrapper_with_padding">
  <div class="clearfix" id="notes">
    unless @notebooks.blank? 
       @notebooks.each do |notebook| 
      
          <div class="note">
            <p class="title">
               link_to notebook.title, notebook_note_path(@notebook) 
            </p>
            <p class="date">
               time_ago_in_words(note.created_at) 
            </p>
          </div>
        </a>
      end 
     else 
      <h2>Add a Note</h2>
      <p>It appears you haven't created any notes yet... Lets fix that. Why don't you go ahead and create a new note.</p>
      <button>
        link_to "New Note", new_notebook_note_path(@note) 
      </button>
     end 
  </div>
</div>
 -->