<h4>Here are your current Notebooks today:  </h4>
    <div class="wrapper_with_padding">
        <div class="clearfix" id="notes">
         <% @notebooks.each do |notebook| %>
            <div class="note">
                <p class="title">    
    	           <%=link_to notebook.title, notebook_path(notebook)%>
    	    		 </p>	
				</div>
					
      				<!--  link_to "Edit", edit_notebook_path(@notebook) class: "button" 
      				 link_to "Delete", notebook_path(@notebook), method: :delete, data: { confirm: "Are you sure?" }, class: "button" 
      			 -->
      	<%end%>
    </div>